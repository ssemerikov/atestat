# Інструкція з розгортання веб-візуалізатора

## Метод 1: GitHub Pages (рекомендовано)

### Крок 1: Підготовка репозиторію

```bash
# Перейдіть до кореневої директорії проєкту
cd /home/cc/claude_code/atestat

# Переконайтеся, що gh-pages директорія має всі файли
ls -la gh-pages/
```

### Крок 2: Налаштування GitHub Pages

1. Зайдіть у налаштування репозиторію на GitHub
2. Перейдіть до розділу **Settings** → **Pages**
3. У розділі **Source**:
   - Оберіть гілку: `main` (або `master`)
   - Оберіть папку: `/gh-pages`
   - Натисніть **Save**

4. Через кілька хвилин сайт буде доступний за адресою:
   ```
   https://[ваш-username].github.io/[назва-репозиторію]/
   ```

### Крок 3: Перевірка даних

Переконайтеся, що симлінк `data/` вказує на правильну директорію:

```bash
cd gh-pages
ls -la data/
# Має відображати файли: all_results.json, methodology.json, etc.
```

## Метод 2: Локальний сервер для тестування

### Використання Python

```bash
cd gh-pages
python3 -m http.server 8000
# Відкрийте http://localhost:8000 у браузері
```

### Використання Node.js

```bash
# Встановіть serve глобально (один раз)
npm install -g serve

# Запустіть сервер
cd gh-pages
serve
# Відкрийте адресу, яку покаже serve
```

### Використання PHP

```bash
cd gh-pages
php -S localhost:8000
# Відкрийте http://localhost:8000 у браузері
```

## Метод 3: Netlify Deploy

### Через Netlify CLI

```bash
# Встановіть Netlify CLI
npm install -g netlify-cli

# Авторизуйтесь
netlify login

# Розгорніть сайт
cd gh-pages
netlify deploy --prod
```

### Через веб-інтерфейс Netlify

1. Зайдіть на https://app.netlify.com/
2. Перетягніть папку `gh-pages` у веб-інтерфейс
3. Сайт буде автоматично розгорнутий

## Метод 4: Vercel Deploy

```bash
# Встановіть Vercel CLI
npm install -g vercel

# Розгорніть сайт
cd gh-pages
vercel --prod
```

## Налаштування CORS (якщо потрібно)

Якщо ви розміщуєте JSON файли на іншому домені, додайте заголовки CORS:

### Для Apache (.htaccess)

```apache
<IfModule mod_headers.c>
    Header set Access-Control-Allow-Origin "*"
</IfModule>
```

### Для Nginx

```nginx
location /data/ {
    add_header Access-Control-Allow-Origin *;
}
```

## Налаштування для GitHub Actions (автоматичне розгортання)

Створіть файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./gh-pages
```

## Перевірка після розгортання

Після розгортання перевірте:

1. ✅ Всі JSON файли завантажуються коректно
2. ✅ CSS стилі застосовуються правильно
3. ✅ JavaScript модулі завантажуються без помилок
4. ✅ Візуалізації відображаються коректно
5. ✅ Вибір ЗВО працює
6. ✅ Порівняння ЗВО функціонує
7. ✅ Експорт у PDF/Excel працює
8. ✅ Адаптивний дизайн на мобільних пристроях

## Вирішення проблем

### Проблема: JSON файли не завантажуються

**Рішення:**
```bash
# Перевірте, чи існують файли
cd gh-pages/data
ls -la

# Якщо файлів немає, скопіюйте їх
cp ../../data/json/*.json .
```

### Проблема: Модулі JavaScript не завантажуються

**Рішення:**
- Переконайтеся, що сервер підтримує MIME-тип `text/javascript` для `.js` файлів
- Перевірте консоль браузера на помилки
- Використайте HTTPS, якщо використовуєте HTTP/2

### Проблема: Стилі не застосовуються

**Рішення:**
- Перевірте шляхи до CSS файлів в `index.html`
- Очистіть кеш браузера (Ctrl+Shift+R)
- Перевірте, чи CSS файли мають правильний MIME-тип

### Проблема: CORS помилки

**Рішення:**
- Розмістіть JSON файли на тому ж домені
- Налаштуйте CORS заголовки на сервері
- Використайте проксі для API запитів

## Оптимізація продуктивності

### 1. Мінімізація файлів

```bash
# CSS
npx cssnano css/main.css > css/main.min.css

# JavaScript
npx terser js/*.js -o js/bundle.min.js
```

### 2. Стиснення зображень

```bash
# Якщо є зображення
npx imagemin images/* --out-dir=images/optimized
```

### 3. Кешування

Додайте до `.htaccess`:

```apache
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType application/json "access plus 1 day"
</IfModule>
```

## Моніторинг

### Google Analytics (опціонально)

Додайте перед `</head>` в `index.html`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=YOUR-GA-ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'YOUR-GA-ID');
</script>
```

## Підтримка

При виникненні проблем:
1. Перевірте консоль браузера (F12)
2. Перевірте мережеві запити у вкладці Network
3. Переконайтеся, що всі залежності завантажені
4. Перевірте, чи правильно налаштовані шляхи до файлів

---

**Успішного розгортання!** 🚀
